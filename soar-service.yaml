apiVersion: v1
kind: Service
metadata:
  name: soar-responder-service
  namespace: monitoring
  labels:
    app: soar-responder
spec:
  type: ClusterIP
  selector:
    app: soar-responder
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
